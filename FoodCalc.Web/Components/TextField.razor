@* Label + InputText + validation message. Use inside an EditForm with
   @bind-Value, exactly like InputText. *@
@using System.Linq.Expressions

<div class="fh-field @CssClass">
	@if (!string.IsNullOrEmpty(Label))
	{
		<label class="form-label" for="@Id">@Label</label>
	}
	<div class="fh-field__control @(ShowRevealToggle ? "fh-field__control--with-toggle" : null)">
		<InputText id="@Id"
		           class="form-control"
		           type="@EffectiveType"
		           placeholder="@Placeholder"
		           autocomplete="@Autocomplete"
		           Value="@Value"
		           ValueChanged="ValueChanged"
		           ValueExpression="ValueExpression"/>
		@if (ShowRevealToggle)
		{
			<button type="button"
			        class="fh-field__reveal"
			        title="@RevealLabel"
			        aria-label="@RevealLabel"
			        aria-controls="@Id"
			        aria-pressed="@(_revealed ? "true" : "false")"
			        @onclick="() => _revealed = !_revealed">
				<i class="bi bi-@(_revealed ? "eye-slash" : "eye")" aria-hidden="true"></i>
			</button>
		}
	</div>
	@if (ValueExpression is not null)
	{
		<ValidationMessage For="ValueExpression"/>
	}
</div>

@code {

	[Parameter]
	public string? Label { get; set; }

	[Parameter]
	public string Id { get; set; } = $"fh-field-{Guid.NewGuid():N}";

	[Parameter]
	public string Type { get; set; } = "text";

	/// <summary>Shows an eye button that toggles the value between hidden and plain text. Only meaningful for Type="password".</summary>
	[Parameter]
	public bool ShowRevealToggle { get; set; }

	[Parameter]
	public string? Placeholder { get; set; }

	[Parameter]
	public string? Autocomplete { get; set; }

	[Parameter]
	public string? CssClass { get; set; }

	[Parameter]
	public string? Value { get; set; }

	[Parameter]
	public EventCallback<string?> ValueChanged { get; set; }

	[Parameter]
	public Expression<Func<string?>>? ValueExpression { get; set; }

	private bool _revealed;

	private string EffectiveType => ShowRevealToggle && _revealed ? "text" : Type;

	private string RevealLabel => _revealed ? UiText.Components.HidePassword : UiText.Components.ShowPassword;

}
